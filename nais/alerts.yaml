apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: rekrutteringsbistand-sms
  namespace: toi
  labels:
    team: toi
spec:
  groups:
    - name: rekrutteringsbistand-sms
      rules:
        - alert: rekrutteringsbistand-sms
          expr: kube_deployment_status_replicas_available{deployment="rekrutteringsbistand-sms"} == 0
          for: 1s
          annotations:
            summary: Appen er nede
            action: "https://logs.adeo.no/app/r/s/l4gq0"
          labels:
            namespace: toi
            severity: critical

        - alert: rekrutteringsbistand-sms
          expr: sum(increase(logd_messages_total{log_app="rekrutteringsbistand-sms",log_level="Error"}[10m])) > 0
          for: 1s
          annotations:
            summary: Appen har logget en error
            action: "https://logs.adeo.no/app/r/s/l4gq0"
          labels:
            namespace: toi
            severity: critical

        - alert: rekrutteringsbistand-sms
          expr: (100 * sum by (log_app, log_namespace) (rate(logd_messages_total{log_app="rekrutteringsbistand-sms  ",log_level=~"Warning"}[5m])) / sum by (log_app, log_namespace) (rate(logd_messages_total{log_app="rekrutteringsbistand-sms  "}[5m]))) > 5
          for: 3m
          annotations:
            summary: Økning i warnings
            action: "https://logs.adeo.no/app/r/s/l4gq0"
          labels:
            namespace: toi
            severity: warning
